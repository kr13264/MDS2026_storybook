import { Meta, Canvas, Controls, Stories } from '@storybook/blocks';
import * as PopoverStories from './index.stories';

<Meta of={PopoverStories} />

# Popover

팝오버 컴포넌트. Tooltip, Info, More 세 가지 서브 컴포넌트를 제공합니다.

---

<div style={{marginBottom: '40px'}} />

## Preview

<Canvas of={PopoverStories.Tooltip} sourceState="hidden" />

## Properties

<Controls of={PopoverStories.Tooltip} />

---

<div style={{marginBottom: '40px'}} />

<Stories includePrimary={false} />
