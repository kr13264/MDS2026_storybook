import { Meta, Canvas, Controls, Stories } from '@storybook/blocks';
import * as VerifiedStories from './Verified.stories';

<Meta of={VerifiedStories} />

# Verified

인증/검증 아이콘 뱃지 컴포넌트입니다.
프로필, 채널명 옆에 인증 상태를 표시합니다.

---

<div style={{marginBottom: '40px'}} />

## Preview

<Canvas of={VerifiedStories.Playground} sourceState="hidden" />

## Properties

<Controls of={VerifiedStories.Playground} />

---

<div style={{marginBottom: '40px'}} />

<Stories includePrimary={false} />
