import { Meta, Canvas, Controls, Stories } from '@storybook/blocks';
import * as SelectionStories from './index.stories';

<Meta of={SelectionStories} />

# Selection

선택 컨트롤 컴포넌트. Check / Add / Radio 3가지 타입과 Circle / Square 형태를 지원합니다.

---

<div style={{marginBottom: '40px'}} />

## Preview

<Canvas of={SelectionStories.Playground} sourceState="hidden" />

## Properties

<Controls of={SelectionStories.Playground} />

---

<div style={{marginBottom: '40px'}} />

<Stories includePrimary={false} />
