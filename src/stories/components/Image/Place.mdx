import { Meta, Canvas, Controls, Stories } from '@storybook/blocks';
import * as PlaceStories from './Place.stories';

<Meta of={PlaceStories} />

# Place

이미지 배치(Place) 상태 컴포넌트. 이미지의 로딩·없음·표시 상태를 시각적으로 구분합니다.

---

<div style={{marginBottom: '40px'}} />

## Preview

<Canvas of={PlaceStories.Playground} sourceState="hidden" />

## Properties

<Controls of={PlaceStories.Playground} />

---

<div style={{marginBottom: '40px'}} />

<Stories includePrimary={false} />
